function setup() {
    createCanvas(windowWidth, windowHeight);
    colorMode(HSB, 100);
    noLoop();
    randomSeed(1);
    noiseSeed(1);
}

function lsystem() {

}

function draw() {
    background(0, 0, 0);
    lsystem();
}